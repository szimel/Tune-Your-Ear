import { useSelector } from "react-redux";


const SessionChord = () => {

  const state = useSelector(state => state)
  console.log(state);
  return (
    <div>
      workinga;lsdjf;alksjdf;lkjasdf
    </div>
  )
};

export default SessionChord;